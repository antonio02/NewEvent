<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newevent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.CadastroConta" />
        <activity android:name=".controller.Login" />
        <activity android:name=".controller.Eventos" />
        <activity android:name=".controller.CriarEvento" />
        <activity android:name=".controller.CriarAtividade" />
        <activity android:name=".controller.Evento"></activity>
    </application>

</manifest>